/* ============================================================
   إعداد الموقع — الملف الوحيد الذي تعدّله

   ضع مفتاح anon من:
   Supabase → Settings → API → Project API keys → anon public

   احفظ الملف وحدّث الصفحة. لا حاجة لإعادة بناء أو رفع.
   ============================================================ */
window.__APP_CONFIG__ = {
  SUPABASE_URL: "https://ykuvsnmddipctrylwxzh.supabase.co",

  // ← الصق المفتاح بين علامتي التنصيص
  SUPABASE_ANON_KEY: "",

  // رابط موقعك بعد ربط النطاق (اتركه فارغاً وسيُستخدم العنوان الحالي)
  APP_URL: ""
};
